# TODO: Implement Hero Section Slideshow

## Steps to Complete:

- [x] Step 1: Update `index.html` to replace the single hero image with a slideshow container containing both images (Herinspirer Logo first, LightHer flyer second).
- [x] Step 2: Update `style.css` to add slideshow styles, including transitions, and ensure mobile responsiveness (adjust height, font sizes, etc.).
- [x] Step 3: Update `script.js` to add JavaScript for slideshow functionality (auto-slide every 3-5 seconds, loop between the two images).
- [ ] Step 4: Test the slideshow by running the website locally and checking responsiveness on mobile devices.

# TODO: Form Modifications

## Steps to Complete:

- [x] Add conditional follow-up fields for Occupation and Education Level when "Other" is selected.
- [x] Add input validation: letters and spaces for text fields (Full Name, Institution, Reason, specify fields), numbers only for Phone Number and Age.
- [x] Make specify fields required when visible.
- [x] Update JavaScript to handle conditional display of specify fields.
- [x] Add JavaScript event listener to handle form submission and redirect to success.html locally for testing.
- [ ] Test the form validation and conditional fields by submitting the form with various inputs.

# TODO: Design Improvements

## Steps to Complete:

- [x] Add beautiful font style to the paragraph in the about section (increased font size, line height, color, max width, margin).
- [x] Adjust countdown section spacing from hero section (increased top padding to 80px).
- [x] Center the countdown h2 title, change its color to #ff2ea6, and enhance styling (font size, weight, margin).
- [x] Update countdown containers to a more stylish design (gradient background, white text, increased padding, border-radius, box-shadow, bold font).
- [x] Fix HTML class attribute typo in countdown section (changed "mclass" to "class" to apply CSS).
- [ ] Test the visual changes by viewing the website locally.
